# pdfpro

Pdfpro is an AI-enhanced PDF chatbot.

1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`

   // for tailwind css
   'npm install tailwindcss @tailwindcss/cli'